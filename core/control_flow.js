// the code will only get executed if the condition is true else the rest part will get executed!!
let a =49
if(a>=48){
    console.log("a is greater than or equal to 48")
}else{
    console.log("a is smaller than 48")
}

/* 1. Basics if-else!
   2. Nested if-else!
   3. else-if (for multiple condition checking!!)
   4. Switch (Same like done in c++!)
   5. Ternary operator (same)!!
*/

// falsy values: false, 0, -0, BigInt, "", null, undefined, Nan(not a number).
// truthy values: "0", 'false', " ", [], {}, function(){}

// Nullish Coalescing Operator (??) : null undefined (it will assign the possible first value to the variable)